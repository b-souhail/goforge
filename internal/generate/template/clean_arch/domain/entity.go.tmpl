package domain

import "time"

type {{.Module}} struct {
    ID        string    `json:"id"`
    CreatedAt time.Time `json:"created_at"`
}

func ({{.Receiver}} {{.Module}}) validateBusinessRules() {}
