package usecases


type {{.Module}}Usecase struct {
	repo repository.{{.Module}}Repo
}

func New{{.Module}}Usecase(repo repository.{{.Module}}Repo) {{.Module}}Usecase {
	return {{.Module}}Usecase{
		repo: repo,
	}
}

func ({{.Receiver}} *{{.Module}}Usecase) Create{{.Module}}(data dtos.{{.Module}}Data) error {
{{.Module}} := entity.{{.Module}}{

}

    	return {{.Receiver}}.repo.Create({{.Module}})
}


func ({{.Receiver}} *{{.Module}}Usecase) Read{{.Module}}(id int) (entity.{{.Module}},error) {
  {{.Module}}Data, err :=  {{.Receiver}}.repo.Read(id)
  if err != nil {
  return entity.{{.Module}}{} , err
  }
 	return {{.Module}}Data , nil
}

func ({{.Receiver}} *{{.Module}}Usecase) Update{{.Module}}(id int ,changes any) error {
    	return {{.Receiver}}.repo.Update(id,changes)
}

func ({{.Receiver}} *{{.Module}}Usecase) Delete{{.Module}}(id int) error {
    	return {{.Receiver}}.repo.Delete(id)
}